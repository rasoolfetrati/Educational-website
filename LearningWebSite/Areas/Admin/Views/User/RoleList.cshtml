@model IList<string>
@{
    ViewData["Title"] = "RoleList";
    Layout = "~/Areas/Admin/Views/Shared/_adminLayout.cshtml";
    int counter = 1;
}

<h3 class="mt-5">نقش های کاربر: @ViewBag.UserName</h3>
<a class="btn btn-primary d-inline" href="/Role">بازگشت به لیست</a>
<hr/>
<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">نام نقش</th>
            <th scope="col"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@counter</td>
                <td>@item</td>
                <td>
                    <form class="form-inline d-inline" asp-area="Admin" asp-controller="User" asp-action="DeleteRole" asp-route-Id="@ViewBag.Id" asp-route-roleName="@item" method="post">
                        <button type="submit" class="btn btn-danger">حذف</button>
                    </form>
                    <a class="btn btn-warning d-inline" href="/EditRole/@ViewBag.Id/@item">ویرایش</a>

                </td>
            </tr>
            counter++;
        }



    </tbody>
</table>